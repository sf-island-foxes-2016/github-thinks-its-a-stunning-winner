<%= form_for @cart_product do |f| %>

  <% if @cart_product.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@cart_product.errors.count, "error") %> prohibited
        this cart_product from being saved:
      </h2>
      <ul>
        <% @cart_product.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :cart_id %><br>
    <%= f.number_field :cart_id %>
  </p>

  <p>
    <%= f.label :product_id %><br>
    <%= f.number_field :product_id %>
  </p>

  <p>
    <%= f.label :quoted_price %><br>
    <%= f.number_field :quoted_price %>
  </p>

  <p>
    <%= f.label :quantity %>
    <%= f.number_field :quantity %>
  </p>

  <p>
    <%= f.submit %>
  </p>

<% end %>
